// TODO: since this is an alias of the uuid package, should we simply alias the types here?
export function uuid(options?: { random: number; rng: number }, buffer?: Array<any> | Buffer, offset?: number): string;
